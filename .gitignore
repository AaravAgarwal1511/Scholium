node_modules
dist
.turbo
.env
.env.local
*.log
*.tsbuildinfo
.DS_Store

# Playwright e2e artifacts (all apps)
test-results/
playwright-report/
playwright/.cache/
blob-report/

# Lighthouse CI reports
.lighthouseci/

# Coverage reports
coverage/

# Google Cloud service-account key (never commit)
gcp-tts-key.json

# Internal legal/content reports (not for public repo)
reports/

# Local Claude Code conversation archive (session transcripts — may hold sensitive context)
/Claude/
.claude/

# past-papers: scripts/build-subject-pages.js regenerates these subject/
# component pages from the live paper_files table on every build (see the
# script's header comment) — pure build output, like dist/. papers.css next
# to them is hand-authored source and stays tracked.
apps/past-papers/public/papers/*/
